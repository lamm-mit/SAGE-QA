[project]
name = "multiagent-qa-project"
version = "0.2.0"
description = "Project-style split of a multi-agent GraphRAG QA notebook."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
