# Graph Maker

The graph maker extracts scientific entities and relations from retrieved paths, source chunks, references, DOIs, figures, and chunk IDs.

It is used internally by `formulate()` / `MindMap.intake()`.
